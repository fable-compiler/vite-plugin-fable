module Greeting

/// Builds the line shown in the page heading.
///
/// Try editing this signature while `vite dev` is running: rename `greet`, or change the
/// parameter type to `int`, and the error should surface against `Greeting.fs` in the browser
/// overlay without you touching the implementation.
val greet : name : string -> string
